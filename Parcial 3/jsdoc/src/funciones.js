/**
 * 
 * @param {Number} numerobase Numero que se va a elevar
 * @param {Number} exponente Potencia a la que se va elevar
 * @returns Number
 */

export function potencia(numerobase,exponente){
    return numerobase*exponente;
}